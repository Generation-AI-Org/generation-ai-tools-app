---
phase: 06-agent-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agent.ts
  - lib/types.ts
  - app/api/chat/route.ts
  - components/chat/MessageList.tsx
  - components/chat/ChatPanel.tsx
autonomous: true
requirements: [CHAT-01, CHAT-02]

must_haves:
  truths:
    - "V2 Member-Chat ruft KB-Tools auf um Antworten zu finden"
    - "Agent nutzt Sonnet Model (nicht Haiku)"
    - "Antworten enthalten Sources-Array mit genutzten KB-Items"
    - "Sources sind im Chat sichtbar unter jeder Antwort"
    - "Agent gibt zu wenn KB keine relevante Info hat"
  artifacts:
    - path: "lib/agent.ts"
      provides: "runAgent function mit Tool-Calling Loop"
      exports: ["runAgent", "SYSTEM_PROMPT"]
    - path: "lib/types.ts"
      provides: "ChatMessage mit sources Feld"
      contains: "sources?: ContentSource[]"
    - path: "app/api/chat/route.ts"
      provides: "V2 Branch mit runAgent"
      contains: "runAgent"
    - path: "components/chat/MessageList.tsx"
      provides: "Sources-Anzeige unter Assistant-Messages"
      contains: "sources"
  key_links:
    - from: "app/api/chat/route.ts"
      to: "lib/agent.ts"
      via: "import runAgent"
      pattern: "import.*runAgent.*from.*agent"
    - from: "lib/agent.ts"
      to: "lib/kb-tools.ts"
      via: "import KB_TOOLS, executeTool"
      pattern: "import.*KB_TOOLS.*executeTool.*from.*kb-tools"
    - from: "components/chat/MessageList.tsx"
      to: "message.sources"
      via: "render sources array"
      pattern: "sources.*map"
---

<objective>
V2 Member-Chat mit Tool-Calling Agent verdrahten.

Purpose: Member bekommen einen intelligenten Agent der die KB selbststaendig erkundet statt Full-Context.

Output:
- `lib/agent.ts` mit runAgent Loop
- Chat-Route Branch fuer V2
- Sources-Anzeige im UI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3-architecture.md

@lib/kb-tools.ts
@lib/llm.ts
@lib/types.ts
@app/api/chat/route.ts
@components/chat/ChatPanel.tsx
@components/chat/MessageList.tsx
</context>

<interfaces>
<!-- Existierende Interfaces die der Executor nutzt -->

From lib/kb-tools.ts:
```typescript
export const KB_TOOLS: Tool[]  // Anthropic Tool-Definitionen
export async function executeTool(name: string, input: Record<string, unknown>): Promise<string>
```

From lib/types.ts:
```typescript
export interface ContentSource {
  slug: string
  title: string
  type: ContentType
}

export interface ChatMessage {
  id: string
  role: 'user' | 'assistant'
  content: string
  recommendedSlugs?: string[]
  created_at: string
  // NEU: sources?: ContentSource[]
}

export type ChatMode = 'public' | 'member'
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Agent-Core erstellen</name>
  <files>lib/agent.ts, lib/types.ts</files>
  <action>
1. **lib/types.ts erweitern:**
   - `ChatMessage` um `sources?: ContentSource[]` ergaenzen

2. **lib/agent.ts erstellen:**
   - System-Prompt aus v3-architecture.md verwenden (exakt kopieren)
   - `runAgent(message: string, history: ChatMessage[])` implementieren
   - Anthropic SDK direkt nutzen (NICHT AI SDK)
   - Model: `claude-sonnet-4-20250514`
   - Max 5 Iterationen (Cost-Limit per CONTEXT.md)
   - Sources tracken: Jeder `kb_read` Call fuegt Item zu sources hinzu
   - Return: `{ text: string, sources: ContentSource[], iterations: number }`

Agent-Loop Logik (aus v3-architecture.md):
```typescript
while (iterations < maxIterations) {
  const response = await client.messages.create({...})
  
  if (response.stop_reason === 'end_turn') {
    // Fertig - Text extrahieren und returnen
  }
  
  if (response.stop_reason === 'tool_use') {
    // Tools ausfuehren
    for (const block of response.content) {
      if (block.type === 'tool_use') {
        const result = await executeTool(block.name, block.input)
        // Bei kb_read: Item zu sources hinzufuegen
      }
    }
    // Tool-Results als naechste Message hinzufuegen
  }
}
```

Sources-Tracking:
- Nur `kb_read` Calls tracken (nicht kb_list/kb_search/kb_explore)
- Aus dem Tool-Result JSON slug, title, type extrahieren
- Duplikate vermeiden (Set oder seen-Check)
  </action>
  <verify>
    <automated>npx tsc --noEmit lib/agent.ts lib/types.ts</automated>
  </verify>
  <done>
    - lib/agent.ts existiert mit runAgent export
    - ChatMessage hat sources Feld
    - TypeScript kompiliert ohne Fehler
  </done>
</task>

<task type="auto">
  <name>Task 2: Chat-Route V2 Branch</name>
  <files>app/api/chat/route.ts</files>
  <action>
1. Import runAgent aus lib/agent.ts
2. Im POST Handler nach mode-Validation:

```typescript
if (validMode === 'member') {
  // V2: Agent mit Tool-Calling
  const result = await runAgent(message, history)
  
  // Assistant-Message persistieren (mit sources)
  await supabase.from('chat_messages').insert({
    session_id: activeSessionId,
    role: 'assistant',
    content: result.text,
    // recommended_slugs bleibt leer fuer V2
  })
  
  return NextResponse.json({
    sessionId: activeSessionId,
    text: result.text,
    recommendedSlugs: [],  // V2 nutzt sources statt recommendedSlugs
    sources: result.sources,
  })
} else {
  // V1: Full-Context (bestehender Code)
  const items = await getFullContent()
  const result = await getRecommendations(message, history, items, validMode)
  // ... rest bleibt wie es ist
}
```

WICHTIG:
- getFullContent() und getRecommendations() NUR im V1-Branch aufrufen
- V2 laesst Full-Content-Load komplett weg (das ist der Performance-Gewinn)
  </action>
  <verify>
    <automated>npx tsc --noEmit app/api/chat/route.ts</automated>
  </verify>
  <done>
    - Route kompiliert
    - mode='member' ruft runAgent auf
    - mode='public' ruft getRecommendations auf (unveraendert)
  </done>
</task>

<task type="auto">
  <name>Task 3: Sources im UI anzeigen</name>
  <files>components/chat/MessageList.tsx, components/chat/ChatPanel.tsx</files>
  <action>
1. **ChatPanel.tsx erweitern:**
   - onHighlight Prop auch fuer Source-Clicks nutzen
   - Sources aus API-Response an Messages weitergeben:
   ```typescript
   const assistantMessage: ChatMessage = {
     // ...existing
     sources: data.sources,  // NEU
   }
   ```

2. **MessageList.tsx erweitern:**
   - Props um `onSourceClick?: (slug: string) => void` ergaenzen
   - Nach Assistant-Message Content: Sources als Pills rendern
   - Styling: Kleine Pills unter der Antwort, clickable
   
UI-Design (Claude's Discretion - schlichte Pills):
```tsx
{msg.role === 'assistant' && msg.sources?.length > 0 && (
  <div className="flex flex-wrap gap-1.5 mt-2 pt-2 border-t border-[var(--border)]/50">
    <span className="text-xs text-text-muted">Quellen:</span>
    {msg.sources.map((source) => (
      <button
        key={source.slug}
        onClick={() => onSourceClick?.(source.slug)}
        className="text-xs px-2 py-0.5 rounded-full bg-[var(--accent)]/10 
                   text-[var(--accent)] hover:bg-[var(--accent)]/20 
                   transition-colors cursor-pointer"
      >
        {source.title}
      </button>
    ))}
  </div>
)}
```

3. **ChatPanel.tsx onSourceClick verdrahten:**
   - MessageList bekommt `onSourceClick={(slug) => onHighlight([slug])}`
   - Damit highlighted der Click auf Source das Item in der Bibliothek
  </action>
  <verify>
    <automated>npx tsc --noEmit components/chat/MessageList.tsx components/chat/ChatPanel.tsx</automated>
  </verify>
  <done>
    - Sources werden unter Assistant-Antworten angezeigt
    - Click auf Source highlighted Item in Bibliothek
    - TypeScript kompiliert
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> API | Untrusted message input |
| API -> Claude | AI response may hallucinate |
| Claude -> KB Tools | Tool results contain user-created content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | runAgent | mitigate | API Key nur server-side, bereits in Anthropic client |
| T-06-02 | Tampering | tool_use input | mitigate | executeTool validiert bereits (Phase 5) |
| T-06-03 | Info Disclosure | KB content | accept | KB ist public content, kein PII |
| T-06-04 | DoS | iteration loop | mitigate | maxIterations=5 hardcoded, per CONTEXT.md |
| T-06-05 | Elevation | mode bypass | mitigate | mode validation bereits in route.ts (Phase 4) |
</threat_model>

<verification>
Nach Plan-Completion:

1. **Type-Check:**
   ```bash
   npx tsc --noEmit
   ```

2. **Manual Test V2:**
   - Als Member einloggen
   - Chat oeffnen (Member Badge sichtbar)
   - Frage stellen: "Was ist ChatGPT?"
   - Antwort sollte Sources zeigen
   - Click auf Source sollte Item highlighten

3. **Manual Test V1 (Regression):**
   - Ausloggen
   - Chat oeffnen (kein Member Badge)
   - Frage stellen
   - Sollte wie vorher funktionieren (recommendedSlugs, keine sources)

4. **Grounding Test:**
   - Frage stellen zu nicht existierendem Topic
   - Agent sollte zugeben: "Dazu habe ich keine Infos"
</verification>

<success_criteria>
- [ ] V2 Chat nutzt runAgent mit Tool-Calling
- [ ] Agent nutzt Sonnet Model
- [ ] Sources werden aus kb_read Calls getracked
- [ ] Sources werden im UI unter Antworten angezeigt
- [ ] Source-Click highlighted Item
- [ ] V1 Chat funktioniert weiterhin (Regression)
- [ ] TypeScript kompiliert ohne Fehler
</success_criteria>

<output>
Nach Completion: `.planning/phases/06-agent-integration/06-01-SUMMARY.md`
</output>
