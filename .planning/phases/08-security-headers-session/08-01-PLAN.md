---
phase: 08-security-headers-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/proxy.ts
  - app/layout.tsx
  - lib/supabase/server.ts
autonomous: true
requirements:
  - SEC-05
  - SEC-06

must_haves:
  truths:
    - "CSP Header blockiert Inline-Scripts ohne Nonce"
    - "CSP Header erlaubt trusted Sources (self, Supabase, Anthropic)"
    - "Nonce ist in Layout lesbar fuer potenzielle Third-Party Scripts"
    - "Cookies haben Secure und SameSite=Lax Flags"
  artifacts:
    - path: "app/proxy.ts"
      provides: "CSP Header mit Nonces"
      contains: "Content-Security-Policy"
    - path: "app/layout.tsx"
      provides: "Nonce aus Header lesbar"
      contains: "x-nonce"
    - path: "lib/supabase/server.ts"
      provides: "Cookie Options (secure, sameSite)"
      contains: "sameSite"
  key_links:
    - from: "app/proxy.ts"
      to: "app/layout.tsx"
      via: "x-nonce Header"
      pattern: "x-nonce"
---

<objective>
CSP Header via Middleware mit Nonces implementieren und Cookie-Security haerten.

Purpose: Browser-Level XSS-Schutz durch CSP und Session-Hijacking-Schutz durch sichere Cookies
Output: CSP-Header auf allen Seiten, Nonces fuer Scripts, sichere Cookie-Einstellungen
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-security-headers-session/08-RESEARCH.md
</context>

<interfaces>
<!-- Bestehende Interfaces die modifiziert werden -->

From app/proxy.ts:
```typescript
import { createClient } from '@/lib/supabase/proxy'
import { NextResponse, type NextRequest } from 'next/server'

export async function proxy(request: NextRequest) {
  // Aktuell nur Supabase Auth Handling
}

export const config = {
  matcher: [...]
}
```

From lib/supabase/server.ts:
```typescript
export async function createClient() {
  // createServerClient mit cookies
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CSP Header via Middleware mit Nonces</name>
  <files>app/proxy.ts</files>
  <action>
Erweitere app/proxy.ts um CSP Header Generation:

1. Nonce generieren mit `crypto.randomUUID()` + Base64-Encoding
2. CSP Header bauen mit:
   - `default-src 'self'`
   - `script-src 'self' 'nonce-{nonce}' 'strict-dynamic'` (+ `'unsafe-eval'` nur in dev)
   - `style-src 'self' 'unsafe-inline'` (Tailwind braucht unsafe-inline)
   - `img-src 'self' blob: data: https://logo.clearbit.com`
   - `font-src 'self'`
   - `connect-src 'self' https://*.supabase.co wss://*.supabase.co https://api.anthropic.com`
   - `object-src 'none'`
   - `base-uri 'self'`
   - `form-action 'self'`
   - `frame-ancestors 'none'`
   - `upgrade-insecure-requests`

3. Nonce in Request-Headers setzen (fuer Layout)
4. CSP Header in Response setzen
5. Bestehenden Supabase Auth Code beibehalten

Matcher so anpassen dass Prefetch-Requests ausgeschlossen werden:
```typescript
export const config = {
  matcher: [
    {
      source: '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
      missing: [
        { type: 'header', key: 'next-router-prefetch' },
        { type: 'header', key: 'purpose', value: 'prefetch' },
      ],
    },
  ],
}
```
  </action>
  <verify>
    <automated>curl -sI http://localhost:3000 | grep -i content-security-policy</automated>
  </verify>
  <done>CSP Header mit Nonce ist in Response auf localhost:3000, blockiert inline scripts</done>
</task>

<task type="auto">
  <name>Task 2: Nonce in Layout lesbar machen</name>
  <files>app/layout.tsx</files>
  <action>
In app/layout.tsx den Nonce aus dem x-nonce Header lesen:

1. Import `headers` from 'next/headers'
2. In RootLayout (async function): `const nonce = (await headers()).get('x-nonce') ?? undefined`
3. Kommentar hinzufuegen wo der Nonce bei Third-Party Scripts genutzt werden kann

WICHTIG: Aktuell keine Third-Party Scripts im Projekt. Der Nonce wird nur vorbereitet.
Der Code aendert sich minimal - nur Header-Read + Variable.
  </action>
  <verify>
    <automated>grep -n "x-nonce" app/layout.tsx</automated>
  </verify>
  <done>Nonce wird aus Header gelesen und ist verfuegbar fuer zukuenftige Script-Tags</done>
</task>

<task type="auto">
  <name>Task 3: Cookie Security fuer Supabase Server Client</name>
  <files>lib/supabase/server.ts</files>
  <action>
In lib/supabase/server.ts die Cookie-Options explizit setzen:

Im `setAll` Callback:
```typescript
cookiesToSet.forEach(({ name, value, options }) =>
  cookieStore.set(name, value, {
    ...options,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    // httpOnly: false ist intentional (Supabase Design)
    // Client braucht Cookies fuer onAuthStateChange()
  })
)
```

WICHTIG per Research:
- httpOnly darf NICHT gesetzt werden (bricht Supabase Client Auth)
- SameSite bleibt 'lax' (nicht 'strict') wegen OAuth Redirects
- secure nur in Production (localhost hat kein HTTPS)
  </action>
  <verify>
    <automated>grep -n "sameSite" lib/supabase/server.ts</automated>
  </verify>
  <done>Cookie-Options secure + sameSite sind explizit gesetzt mit Dokumentation warum httpOnly fehlt</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Server | User-Requests mit potentiell bösartigem Content |
| Third-Party Scripts | (aktuell keine) Externe Scripts die Nonces brauchen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | XSS via Inline Script | mitigate | CSP mit Nonces blockiert Inline Scripts ohne Nonce |
| T-08-02 | Information Disclosure | Session Cookie Theft | mitigate | Secure + SameSite Flags auf Cookies |
| T-08-03 | Tampering | Clickjacking | mitigate | X-Frame-Options: DENY (bereits in next.config.ts) |
| T-08-04 | Information Disclosure | Data Exfiltration | mitigate | CSP connect-src Whitelist |
</threat_model>

<verification>
Nach allen Tasks:
1. `curl -sI http://localhost:3000 | grep -i security` zeigt CSP und andere Headers
2. Browser DevTools -> Network -> Response Headers zeigt Content-Security-Policy
3. Browser Console zeigt keine CSP Violations auf normaler Seitennutzung
4. Login-Flow funktioniert noch (Cookie-Settings brechen nichts)
</verification>

<success_criteria>
- CSP Header in allen Responses
- Nonce-basierte Script-Policy (kein unsafe-inline fuer scripts)
- Cookie-Options dokumentiert und gesetzt
- Keine Regression im Auth-Flow
</success_criteria>

<output>
After completion, create `.planning/phases/08-security-headers-session/08-01-SUMMARY.md`
</output>
